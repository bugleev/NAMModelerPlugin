name: Release Native (disabled)

# Disabled: used deprecated create-release with insufficient token permissions,
# and duplicated Windows packaging already covered by Build Windows + manual gh release.
# Mac artifacts come from the Build macOS workflow (unsigned zip for OSS users).
on:
  workflow_dispatch:

env:
  PROJECT_NAME: NeuralAmpModeler

jobs:
  noop:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "This workflow is intentionally disabled."
          echo "Use Build Windows / Build macOS instead."
